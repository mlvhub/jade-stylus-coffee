h2 Todos

h4 Create Todo

form
  input(type='text', ng-model='todo.title')
  textarea(ng-model='todo.description')
  button(ng-click='save(todo)') Submit

div(ng-repeat='todo in todos')
  p {{todo.title}}
